<!-- Use `npm run lint` and `npm run build` before PR -->
## What
<!-- Why this is meaningful, not forceful -->

## How
<!-- `localStorage` only? No new `fetch` to external? Tested Win `Ctrl` + Mac `⌘`? -->

## Screenshots
<!-- If UI: before/after, theme Ink/Paper -->

## Checklist
- [ ] `npm run lint` passes
- [ ] `npm run build` passes (`tsc -b && vite build`)
- [ ] No new backend/auth — stays `Privacy-first · Local storage only`
- [ ] Added to `README` features table if user-facing
